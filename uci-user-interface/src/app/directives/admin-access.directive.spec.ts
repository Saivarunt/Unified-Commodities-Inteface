import { AdminAccessDirective } from './admin-access.directive';

describe('AdminAccessDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminAccessDirective();
    expect(directive).toBeTruthy();
  });
});
