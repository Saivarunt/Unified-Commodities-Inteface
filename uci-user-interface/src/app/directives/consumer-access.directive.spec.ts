import { ConsumerAccessDirective } from './consumer-access.directive';

describe('ConsumerAccessDirective', () => {
  it('should create an instance', () => {
    const directive = new ConsumerAccessDirective();
    expect(directive).toBeTruthy();
  });
});
