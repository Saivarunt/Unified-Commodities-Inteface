import { SupplierAccessDirective } from './supplier-access.directive';

describe('SupplierAccessDirective', () => {
  it('should create an instance', () => {
    const directive = new SupplierAccessDirective();
    expect(directive).toBeTruthy();
  });
});
