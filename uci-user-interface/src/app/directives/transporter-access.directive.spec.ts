import { TransporterAccessDirective } from './transporter-access.directive';

describe('TransporterAccessDirective', () => {
  it('should create an instance', () => {
    const directive = new TransporterAccessDirective();
    expect(directive).toBeTruthy();
  });
});
